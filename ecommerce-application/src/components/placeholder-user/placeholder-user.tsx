import './placeholder-user.css';

const PlaceholderUser = () => {
    return (
        <div className='placeholder-user_wrapper'>
            <span className='placeholder-user_text'>Start by selecting the required category in the side menu.</span>
        </div>
    );
};

export default PlaceholderUser;